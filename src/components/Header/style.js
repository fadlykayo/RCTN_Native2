import {
  Ratio
} from '../../helpers';

export default {
  container: {
    height: 50,
    width: Ratio.screenWidth,
    paddingTop: 17,
    backgroundColor: 'red',
    alignItems: 'center'
  },

  text: {
    fontSize: 20,
  }
};
