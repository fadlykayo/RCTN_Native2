import {
  Ratio
} from '../../helpers';

export default {
  container: {
    height: 60,
    width: Ratio.screenWidth,
    padding: 20,
    backgroundColor: 'blue',
    alignItems: 'center',
    justifyContent: 'center',
  },

  text: {
    fontSize: 20,
  }
};
